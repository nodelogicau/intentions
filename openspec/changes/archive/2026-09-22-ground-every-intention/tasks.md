## 1. Specs

- [x] 1.1 `openspec validate --changes`: every MODIFIED header resolves against the main specs

## 2. README

- [x] 2.1 The serves graph paragraph: every intention reaches a firm terminus of its own subject; reachability, not presence; unserved is a warning in this revision and refusal later
- [x] 2.2 The same paragraph or the next: a terminus is firm and the person's word; a harness may draft one tentative and it grounds nothing until firmed; no policy applies to a terminus
- [x] 2.3 Policies paragraph: conditions apply only to intentions that are not termini
- [x] 2.4 Validation section: the warning and the info finding, and that a write producing a warning is accepted and reports it
- [x] 2.5 Design Principles: add "The why is the price of entry" after "A terminus is a person, not a task"
- [x] 2.6 Status: the transition note, warning now and refusal at the next file-breaking revision
- [x] 2.7 Reference Tool Set: the `intention_firm` bullet gains the terminus clause; the `intention_add` row's description notes that an intention should serve a terminus

## 3. Example workspace and site

- [x] 3.1 `docs/example`: `intention edit` the one-to-one with `--serves <terminus>:for-the-sake-of`; confirm every intention now reaches the terminus
- [x] 3.2 Regenerate the proof figure and confirm the candidate set is unchanged
- [x] 3.3 Read the page against the README; nothing on the page changes meaning

## 4. Close out

- [x] 4.1 Commit and push; confirm Pages rebuilt
- [x] 4.2 Open an issue on intentions-cli: the unserved warning in `validate` and on write, the terminus firmness rules, `intention_firm` refusing a policy on a terminus, the draft-terminus info finding, and a skill that walks an existing workspace up to its termini by asking
- [x] 4.3 Close #26 with a comment naming the commit and how the answer differs from the proposal: warning now, refusal later; a terminus is firm and the person's rather than refused from a harness; the subject match
- [x] 4.4 Archive with sync so the three deltas land
- [x] 4.5 Knowledge workspace: two claims and a synthesis on the spine; commit and push
