## 1. Apply wording

- [x] 1.1 Confirm the RENAMED FROM header matches `openspec/specs/object-model/spec.md` exactly
- [x] 1.2 Run `openspec validate --changes` and confirm the change passes

## 2. Archive

- [x] 2.1 Archive with sync so the rename and rewordings land in the main specs
- [x] 2.2 Grep the main specs for "terminal status" and "its status" and confirm no matches remain
