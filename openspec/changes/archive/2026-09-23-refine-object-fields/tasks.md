## 1. Specs

- [x] 1.1 `openspec validate --changes`: every MODIFIED header resolves

## 2. README

- [x] 2.1 COMMITMENT: the example (`origin: resolution` and `resolution: res_…`), the `origin` row and a new `resolution` row, the projection line, the Two origins paragraph if it names the shape, and the object-model diagram line
- [x] 2.2 INTENTION `parties` row and COMMITMENT `parties` row: the two shapes, and the two senses of `tentative`
- [x] 2.3 Source and timestamp: the person's-act convention, and `timestamp` as last write on an object
- [x] 2.4 DESIRE: `parties` row, in the example if natural, the "carries none of what makes an intention" sentence, the adoption paragraph, and the `desire_add` and `desire_edit` tool rows
- [x] 2.5 Harmonise the `version` and `subject` rows across the four tables

## 3. Close out

- [x] 3.1 Commit and push
- [x] 3.2 Open an issue on intentions-cli: `origin` and `resolution` on write, read and validate; `parties` on desires; `timestamp` on edit
- [x] 3.3 Archive with sync
- [x] 3.4 Knowledge workspace: one claim and a qualification synthesis; commit and push
