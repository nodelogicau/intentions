## 1. Specs

- [x] 1.1 `openspec validate --changes`: the new capability and both MODIFIED deltas resolve

## 2. README

- [x] 2.1 Core Object Types: a `DESIRE` section before `INTENTION`, opening with the ladder desire, intention, commitment; a complete YAML example; the fields table in canonical order; the exemptions; retirement kinds and adoption; the projection
- [x] 2.2 Identifiers table: `des_`; Records: the kinds-per-type table gains desire and the `adopted_as` rule beside `superseded_by`
- [x] 2.3 Object Model diagram and the serves-graph paragraph: a DESIRE row above INTENTION with `adopted_as` pointing down; a sentence that a desire serves only a terminus, possibly a draft, and is not grounded by it
- [x] 2.4 File Layout: `/desires/`; `index.yaml` sentence unchanged but the example gains a desire entry if the layout shows entries
- [x] 2.5 Validation section: the three desire errors
- [x] 2.6 Design Principles, under "The why is the price of entry": where a passing remark goes instead
- [x] 2.7 Reference Tool Set: a Desire group of six rows, and a bullet for `desire_adopt`'s order of operations
- [x] 2.8 Format name and type names wherever the three object types are enumerated

## 3. Close out

- [ ] 3.1 Commit and push; confirm the page is unaffected
- [ ] 3.2 Open an issue on intentions-cli: the object, six verbs and tools, `resolve` and `unresolved` ignoring desires, validation, and the skill's advice that a passing remark is `desire add`
- [ ] 3.3 Archive with sync so `desire` lands and the two deltas apply
- [ ] 3.4 Knowledge workspace: qualify the Desire particular's synthesis to built, closing the two shape questions; commit and push
